services:
  web:
    build: .
    container_name: revelations-web
    restart: unless-stopped
    environment:
      NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-https://staging.revelations.me}
    ports:
      - "127.0.0.1:3001:3000"
