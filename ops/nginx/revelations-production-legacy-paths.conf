# Case-sensitive legacy paths for the canonical HTTPS server block.
#
# Keep this rule in Nginx. Do not move /About into Next.js redirects:
# Next.js matching is case-insensitive and would create a redirect loop.

location = /About {
    return 301 https://revelations.me/about;
}
