import SiteHeader from '@/components/site-header'; import SiteFooter from '@/components/site-footer'; import ArticleCard from '@/components/article-card'; import { getArticlesBySection } from '@/lib/articles'; import { SECTIONS } from '@/lib/sections';
const SECTION = 'people';
export function generateMetadata(){const s=SECTIONS[SECTION];return {title:s.title,description:s.description,alternates:{canonical:`/${SECTION}`}}}
export default function Page(){const s=SECTIONS[SECTION];const items=getArticlesBySection(SECTION);return <div className="min-h-screen"><SiteHeader/><main className="pt-48 md:pt-56 pb-24 max-w-7xl mx-auto px-6 md:px-12"><span className="font-mono text-[11px] tracking-[.25em] uppercase text-rose">{s.subtitle}</span><h1 className="font-display text-6xl md:text-8xl mt-5 mb-5">{s.title}</h1><p className="font-body text-muted-foreground max-w-2xl mb-16">{s.description}</p><div className="grid md:grid-cols-2 gap-12">{items.map((a,i)=><ArticleCard key={a.slug} article={a} large={i<2}/>)}</div></main><SiteFooter/></div>}
